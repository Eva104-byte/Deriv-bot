# ─────────────────────────────────────────────────────────────
#  Deriv Differs Bot — Environment Variables
#  Copy this file to .env for local testing (never commit .env)
#  On Railway: paste these into the "Variables" tab instead
# ─────────────────────────────────────────────────────────────

# ── REQUIRED ─────────────────────────────────────────────────
DERIV_API_TOKEN=YOUR_API_TOKEN_HERE

# ── Starting market (optional) ────────────────────────────────
MARKET=1HZ50V

# ── Digit Differs Bot ─────────────────────────────────────────
DIFFERS_ENABLED=true
DIFFERS_STAKE=1.00
DIFFERS_DURATION=1
DIFFERS_MODE=auto
# DIFFERS_MANUAL_DIGIT=3      # only needed if DIFFERS_MODE=manual
DIFFERS_TARGET_PROFIT=10.00
DIFFERS_STOP_LOSS=5.00
# ── Differs Martingale ────────────────────────────────────────
DIFFERS_MARTINGALE=false
DIFFERS_MARTINGALE_MULT=2.0   # stake multiplier after each loss
DIFFERS_MARTINGALE_MAX=0      # max stake cap in USD (0 = no cap)

# ── Even / Odd Bot ────────────────────────────────────────────
EO_ENABLED=false
EO_STAKE=1.00
EO_DURATION=1
EO_TARGET_PROFIT=10.00
EO_STOP_LOSS=5.00
# ── E/O Martingale ────────────────────────────────────────────
EO_MARTINGALE=false
EO_MARTINGALE_MULT=2.0        # stake multiplier after each loss
EO_MARTINGALE_MAX=0           # max stake cap in USD (0 = no cap)

# ── Auto Digit Selector ───────────────────────────────────────
ADS_ENABLED=true
ADS_TICK_SAMPLE=1000
ADS_THRESHOLD=8.5
ADS_SCAN_INTERVAL=300
# ADS_MARKETS=1HZ10V,1HZ25V,1HZ50V,1HZ100V   # comma-separated, overrides default list

# ── Deriv App ID (leave default unless you registered your own) ─
# DERIV_APP_ID=1089
